import("//build/config/android/rules.gni")

android_assets("extension_assets") {
  disable_compression = true
  renaming_sources = [
    "bundled.json",
    "purebeat.crx",
  ]
  renaming_destinations = [
    "extensions/bundled.json",
    "extensions/purebeat.crx",
  ]
}
